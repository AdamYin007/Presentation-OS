/**
 * @awe/audience-engine — Public API
 */
"use strict";

const { adaptDeck } = require("@awe/presentation-audience-engine");
const { SPEAKER_PROFILES, AUDIENCE_ROLES, ADAPTATION_DIMENSIONS } = require("@awe/presentation-audience-engine");

module.exports = {
  adaptDeck,
  SPEAKER_PROFILES,
  AUDIENCE_ROLES,
  ADAPTATION_DIMENSIONS,
};
